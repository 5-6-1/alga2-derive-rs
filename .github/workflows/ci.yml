name: CI

on:
  push:
    branches: [master]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  fmt:
    name: fmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --check

  clippy:
    name: clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo clippy --all-targets -- -D warnings

  test:
    name: test (stable + MSRV)
    runs-on: ubuntu-latest
    strategy:
      matrix:
        rust: [stable, 1.93.0]
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@${{ matrix.rust }}
      - uses: Swatinem/rust-cache@v2
      # Unit tests only: the doctests expand against alga2, whose local
      # path dev-dep exists only on the maintainer's machine. Once alga2
      # 0.1.0 is on crates.io, switch this to plain `cargo test` and the
      # doctests run here too.
      - run: cargo test --lib
